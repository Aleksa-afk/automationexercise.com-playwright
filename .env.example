# Credentials for a pre-registered automationexercise.com account (login + verifyLogin tests).
LOGIN_EMAIL=
LOGIN_PASSWORD=

# Optional: override the target environment (defaults to https://automationexercise.com).
# BASE_URL=
